
const config = {
    canvas: {
        width: 640,
        height: 360
    },
    camera: {
        moveDestSpeed: 10,
        speedModifier: 0.9
    }
}


canvas.width = config.canvas.width;
canvas.height = config.canvas.height;
